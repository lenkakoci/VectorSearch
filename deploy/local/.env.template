# Local Docker Compose environment for the geological report pipeline.
# Copy to .env and fill in a real password. Never commit .env.

PGDATABASE=geodb
PGUSER=admin
PGPASSWORD=change-me

# Consumed by data/scripts/*.py when connecting from the host.
PGHOST=localhost
PGPORT=5432
